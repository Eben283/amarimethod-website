import { OPS_SURFACE_NAV_CSS, opsEmbedBootScript, opsSurfaceNavHtml } from "../../functions/lib/ops-surface-nav.js";

const CLIENT_DESK_HTML = `<!doctype html>
<html lang="en"><head>
<meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="robots" content="noindex, nofollow" /><title>Amari Client Desk</title>
<style>
  :root { color-scheme: dark; font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; background: #101917; color: #eaf0eb; }
  * { box-sizing: border-box; } body { margin: 0; min-height: 100vh; background: linear-gradient(135deg, #11201c 0%, #101917 48%, #172321 100%); }
  main { width: min(1160px, calc(100% - 32px)); margin: auto; padding: 48px 0 72px; } .eyebrow { color: #9edbb4; font-size: .72rem; font-weight: 800; letter-spacing: .15em; text-transform: uppercase; }
  h1 { max-width: 720px; margin: 10px 0 12px; font-family: Georgia, "Times New Roman", serif; font-size: clamp(2.6rem, 6vw, 5rem); font-weight: 400; letter-spacing: -.055em; line-height: .94; }
  .lede { max-width: 680px; margin: 0; color: #a9bcb0; line-height: 1.55; } .notice { margin: 22px 0 28px; color: #b8dbc2; font-size: .83rem; }
  .tools { display: flex; flex-wrap: wrap; align-items: center; gap: 10px; margin: 28px 0 18px; } input, select { min-height: 44px; border: 1px solid #365646; border-radius: 10px; background: #14221b; color: #f0f8f1; font: inherit; }
  input { flex: 1 1 300px; padding: 10px 13px; } select { padding: 9px 10px; } input:focus, select:focus, button:focus { outline: 2px solid #8fe0a9; outline-offset: 2px; }
  .status { margin-left: auto; color: #91a89a; font-size: .8rem; } .layout { display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(330px, .9fr); gap: 15px; }
  .shell { overflow: hidden; border: 1px solid #2f4438; border-radius: 16px; background: rgba(19, 31, 25, .88); box-shadow: 0 18px 50px rgba(0,0,0,.16); }
  .list { margin: 0; padding: 0; list-style: none; } .row { width: 100%; display: grid; grid-template-columns: 10px minmax(0, 1fr) auto; gap: 12px; align-items: start; padding: 15px; border: 0; border-bottom: 1px solid #283a30; background: transparent; color: inherit; cursor: pointer; font: inherit; text-align: left; }
  .row:hover, .row:focus-visible { background: #1a2b22; } .signal { width: 8px; height: 8px; margin-top: 7px; border-radius: 50%; background: #62796a; } .signal.inbound { background: #e5a05e; box-shadow: 0 0 0 4px rgba(229,160,94,.13); }
  .name { display: block; font-size: .94rem; font-weight: 750; } .preview { display: -webkit-box; overflow: hidden; margin-top: 4px; color: #afc0b4; font-size: .81rem; line-height: 1.4; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }
  .meta { max-width: 145px; color: #8da594; font-size: .72rem; line-height: 1.45; text-align: right; } .empty { padding: 32px 18px; color: #9aac9f; line-height: 1.5; }
  .profile { min-height: 500px; padding: 24px; } .profile h2 { margin: 0; font-family: Georgia, "Times New Roman", serif; font-size: 1.8rem; font-weight: 400; letter-spacing: -.03em; } .profile p { color: #a8baae; line-height: 1.5; }
  .facts { display: flex; flex-wrap: wrap; gap: 7px; margin: 18px 0 24px; } .fact { padding: 6px 8px; border-radius: 999px; background: #243a2d; color: #cae4d1; font-size: .73rem; } .section { margin-top: 24px; } .section h3 { margin: 0 0 9px; color: #dceadd; font-size: .77rem; letter-spacing: .08em; text-transform: uppercase; }
  .timeline { display: grid; gap: 9px; } .event { padding: 10px; border-left: 2px solid #426c50; background: #17261e; } .event.inbound { border-color: #e5a05e; } .event strong, .event span { display: block; } .event strong { color: #e7f1e9; font-size: .78rem; } .event span { margin-top: 3px; color: #a9b9ae; font-size: .75rem; line-height: 1.4; }
  .quiet { color: #91a89a; font-size: .83rem; } @media (max-width: 760px) { main { padding-top: 32px; } .layout { grid-template-columns: 1fr; } .profile { min-height: 0; } .status { width: 100%; margin-left: 0; } }
  /*__OPS_SURFACE_NAV_CSS__*/
</style></head><body><script>__OPS_EMBED_BOOT__</script><main>__OPS_SURFACE_NAV__
<div class="eyebrow">Read-only client workspace</div><h1>Client Desk</h1><p class="lede">A calm view of every client relationship: the last real communication, who has replied, and the context needed before you open GoHighLevel.</p>
<p class="notice">The desk never sends, books, or edits records. GoHighLevel remains the source and sender.</p>
<div class="tools"><input id="query" type="search" autocomplete="off" placeholder="Search name, email, or phone" aria-label="Search clients" /><select id="scope" aria-label="Directory scope"><option value="clients">Client records</option><option value="all">All mirrored contacts</option></select><span class="status" id="status">Loading client records…</span></div>
<div class="layout"><section class="shell" aria-label="Client list"><ul class="list" id="list"></ul></section><aside class="shell profile" id="profile"><h2>Select a client</h2><p>Choose a client to see their latest communication and read-only relationship history.</p></aside></div>
</main><script>
(() => { const list = document.getElementById('list'), profile = document.getElementById('profile'), query = document.getElementById('query'), scope = document.getElementById('scope'), status = document.getElementById('status'); let timer, selected = null;
 const ago = (value) => { if (!value) return 'No communication mirrored'; const days = Math.floor((Date.now() - new Date(value).getTime()) / 86400000); return days <= 0 ? 'Today' : days === 1 ? 'Yesterday' : days + 'd ago'; };
 const label = (row) => row.last_direction === 'inbound' ? 'Client replied' : row.last_direction === 'outbound' ? 'Last sent' : 'No communication';
 const escape = (value) => String(value || '').replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]);
 async function load() { const params = new URLSearchParams({ limit: '50', scope: scope.value }); if (query.value.trim().length >= 2) params.set('query', query.value.trim()); status.textContent = 'Loading…'; const res = await fetch('/client-desk/contacts?' + params, { credentials: 'same-origin' }); if (!res.ok) { status.textContent = 'Client list unavailable.'; return; } const payload = await res.json(); list.replaceChildren(); status.textContent = payload.contacts.length + ' record' + (payload.contacts.length === 1 ? '' : 's') + (scope.value === 'clients' ? ' with activity' : ''); if (!payload.contacts.length) { list.innerHTML = '<li class="empty">No matching client records.</li>'; return; } for (const row of payload.contacts) { const item = document.createElement('li'); const button = document.createElement('button'); button.className = 'row'; button.innerHTML = '<i class="signal ' + (row.last_direction === 'inbound' ? 'inbound' : '') + '"></i><span><strong class="name">' + escape(row.display_name || 'Unnamed client') + '</strong><span class="preview">' + escape(row.last_preview || 'No communication has been mirrored for this client.') + '</span></span><span class="meta">' + label(row) + '<br>' + ago(row.last_occurred_at) + (row.next_appointment_at ? '<br>Next: ' + escape(new Date(row.next_appointment_at).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })) : '') + '</span>'; button.addEventListener('click', () => show(row.id)); item.append(button); list.append(item); } }
 async function show(id) { selected = id; profile.innerHTML = '<p class="quiet">Loading client record…</p>'; const res = await fetch('/client-desk/contacts/' + encodeURIComponent(id) + '?limit=12', { credentials: 'same-origin' }); if (!res.ok) { profile.innerHTML = '<p class="quiet">This client record could not be loaded.</p>'; return; } const data = await res.json(), c = data.contact; const communications = data.communications || []; const messages = communications.length ? communications.map((m) => '<div class="event ' + (m.direction === 'inbound' ? 'inbound' : '') + '"><strong>' + (m.direction === 'inbound' ? 'Client' : 'Sent') + ' · ' + escape(m.channel || 'message') + ' · ' + escape(ago(m.occurred_at)) + '</strong><span>' + escape(m.subject_or_preview || 'No preview available') + '</span></div>').join('') : '<p class="quiet">No communication has been mirrored for this client.</p>'; const facts = [c.email_normalized, c.phone_e164, data.importedCurrentState?.series_type, data.importedCurrentState?.sessions_remaining != null ? data.importedCurrentState.sessions_remaining + ' sessions remaining' : null, data.nextAppointment ? 'Next appointment ' + new Date(data.nextAppointment.starts_at).toLocaleDateString(undefined, { month: 'short', day: 'numeric' }) : null].filter(Boolean).map((v) => '<span class="fact">' + escape(v) + '</span>').join(''); profile.innerHTML = '<h2>' + escape(c.display_name || 'Unnamed client') + '</h2><div class="facts">' + (facts || '<span class="fact">Imported client record</span>') + '</div><section class="section"><h3>Communication</h3><div class="timeline">' + messages + '</div></section><section class="section"><h3>Appointments</h3><div class="timeline">' + ((data.appointments || []).slice(0, 4).map((a) => '<div class="event"><strong>' + escape(new Date(a.starts_at).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })) + ' · ' + escape(a.status || 'recorded') + '</strong><span>' + escape(a.service_name || 'Service not mapped') + '</span></div>').join('') || '<p class="quiet">No appointments mirrored.</p>') + '</div></section>'; }
 query.addEventListener('input', () => { clearTimeout(timer); timer = setTimeout(load, 220); }); scope.addEventListener('change', load); load(); })();
</script></body></html>`;

export function clientDeskHtml() { return CLIENT_DESK_HTML.replaceAll("__OPS_SURFACE_NAV__", opsSurfaceNavHtml("Client Desk")).replaceAll("__OPS_SURFACE_NAV_CSS__", OPS_SURFACE_NAV_CSS).replaceAll("__OPS_EMBED_BOOT__", opsEmbedBootScript()); }
