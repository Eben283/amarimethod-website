// The Desk is the complete staff communication surface: every mirrored contact
// is indexed by latest activity, and the selected record preserves client,
// automated, and operational chronology. It remains a read-only mirror.
const CLIENT_DESK_HTML = `<!doctype html>
<html lang="en"><head>
<meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="robots" content="noindex, nofollow" /><title>Amari Communication</title>
<style>
  .message-link { color: #2769aa; font-weight: 800; text-decoration: underline; text-decoration-color: rgba(39, 105, 170, .38); text-underline-offset: 2px; } .message-link:hover { text-decoration-color: currentColor; }
  :root { color-scheme: light; font-family: "DM Sans", ui-sans-serif, system-ui, sans-serif; color: #243f49; background: #f4f7f6; }
  * { box-sizing: border-box; } body { margin: 0; min-width: 320px; background: #f4f7f6; } button, input, select, textarea { font: inherit; } button { color: inherit; } main { width: min(1480px, calc(100% - 34px)); margin: 0 auto; padding: 30px 0 54px; }
  .eyebrow { color: #71858b; font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 11px; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; } .page-head { display: flex; align-items: end; justify-content: space-between; gap: 24px; margin: 10px 0 20px; } h1 { margin: 0; font-family: "Bona Nova", Georgia, serif; font-size: clamp(2.15rem, 4vw, 3.5rem); font-weight: 400; letter-spacing: -.055em; line-height: .96; } .page-note { max-width: 430px; margin: 0; color: #657b82; font-size: 13px; line-height: 1.48; text-align: right; }
  .searchbar { display: flex; align-items: center; gap: 12px; padding: 11px 14px; border: 1px solid #d3e0df; border-radius: 12px; background: #fff; box-shadow: 0 8px 24px rgba(28, 61, 70, .045); } .searchbar svg { flex: 0 0 auto; color: #72878b; } .searchbar input { width: 100%; border: 0; outline: 0; color: #29454e; font-size: 15px; } .searchbar input::placeholder { color: #91a0a3; } .count { flex: 0 0 auto; color: #71858b; font-size: 12px; white-space: nowrap; }
  .workspace { display: grid; grid-template-columns: minmax(255px, .72fr) minmax(390px, 1.3fr) minmax(280px, .84fr); height: clamp(560px, calc(100vh - 230px), 840px); margin-top: 18px; overflow: hidden; border: 1px solid #d5e2e0; border-radius: 18px; background: #fff; box-shadow: 0 17px 48px rgba(28, 61, 70, .09); } .pane { min-width: 0; min-height: 0; } .inbox { overflow: auto; border-right: 1px solid #e4eceb; background: #fcfdfd; } .conversation { display: flex; min-height: 0; flex-direction: column; border-right: 1px solid #e4eceb; background: #fff; } .record { overflow: auto; background: #f9fbfa; }
  .pane-head { display: flex; align-items: center; justify-content: space-between; min-height: 67px; padding: 18px 19px 14px; border-bottom: 1px solid #e7eeec; } .pane-title { margin: 0; color: #2d4a53; font-family: "Bona Nova", Georgia, serif; font-size: 22px; font-weight: 400; letter-spacing: -.025em; } .unread { color: #1d5bb7; font-size: 12px; font-weight: 800; }
  .thread-list { margin: 0; padding: 0; list-style: none; } .thread-row { position: relative; display: grid; grid-template-columns: 12px minmax(0, 1fr) auto; width: 100%; gap: 10px; padding: 15px 14px 15px 17px; border: 0; border-bottom: 1px solid #e8efee; background: transparent; cursor: pointer; text-align: left; } .thread-row > span:nth-child(2) { min-width: 0; } .thread-row:hover { background: #f2f7f6; } .thread-row[aria-current="true"] { background: #edf5f3; } .thread-row[aria-current="true"]::before { position: absolute; inset: 0 auto 0 0; width: 3px; background: #2d78f5; content: ""; } .thread-row:focus-visible, .show-all:focus-visible { outline: 2px solid #2d78f5; outline-offset: -2px; } .blue-dot { width: 8px; height: 8px; margin-top: 5px; border-radius: 50%; background: transparent; } .blue-dot.unread { background: #2d78f5; box-shadow: 0 0 0 4px rgba(45,120,245,.12); } .thread-name { display: block; overflow: hidden; color: #294650; font-size: 14px; font-weight: 800; text-overflow: ellipsis; white-space: nowrap; } .thread-preview { display: -webkit-box; max-height: 2.76em; min-width: 0; overflow: hidden; margin-top: 4px; color: #71848a; font-size: 12px; line-height: 1.38; overflow-wrap: anywhere; -webkit-box-orient: vertical; -webkit-line-clamp: 2; } .thread-meta { color: #839398; font-size: 11px; line-height: 1.55; text-align: right; white-space: nowrap; } .thread-exact-time { display: block; color: #9aa7ab; font-size: 10px; font-weight: 600; line-height: 1.3; } .list-empty { padding: 30px 18px; color: #71848a; font-size: 13px; line-height: 1.5; }
  .conversation-empty { display: grid; min-height: 620px; place-items: center; padding: 42px; color: #526b72; text-align: center; } .conversation-empty strong { display: block; margin-bottom: 7px; color: #36535c; font-family: "Bona Nova", Georgia, serif; font-size: 25px; font-weight: 400; }
  .client-head { padding: 16px 20px; border-bottom: 1px solid #e7eeec; } .client-head-row { display: flex; align-items: center; gap: 11px; } .avatar { display: grid; width: 35px; height: 35px; flex: 0 0 auto; place-items: center; border-radius: 50%; background: #dce9f9; color: #356493; font-size: 12px; font-weight: 800; } .client-name { margin: 0; color: #294650; font-size: 16px; font-weight: 800; } .client-address { margin: 2px 0 0; color: #788b90; font-size: 12px; } .send-state { margin: 11px 0 0; color: #74878c; font-size: 11px; } .send-state b { color: #c66a54; font-weight: 800; }
  .timeline-toolbar { display: flex; gap: 6px; overflow-x: auto; padding: 10px 16px; border-bottom: 1px solid #e7eeec; background: #fcfdfd; } .timeline-filter { flex: 0 0 auto; border: 1px solid #d6e3e0; border-radius: 999px; padding: 5px 8px; background: #fff; color: #456068; cursor: pointer; font-size: 12px; font-weight: 800; } .timeline-filter:hover, .timeline-filter[aria-pressed="true"] { border-color: #b9d2cc; background: #eaf4f1; color: #315a55; } .timeline-filter:focus-visible { outline: 2px solid #2d78f5; outline-offset: 1px; } .timeline-scroll { min-height: 0; flex: 1; overflow: auto; } .timeline { display: grid; gap: 11px; padding: 20px; } .day-line { display: flex; align-items: center; gap: 9px; margin: 4px 0 1px; color: #62777d; font-size: 12px; font-weight: 700; text-align: center; } .day-line::before, .day-line::after { height: 1px; flex: 1; background: #e5eceb; content: ""; } .message { max-width: 92%; padding: 11px 12px; border: 1px solid #e2eae8; border-radius: 13px 13px 13px 4px; background: #f4f8f7; color: #395861; justify-self: start; } .message.outbound { border: 0; border-radius: 13px 13px 4px 13px; background: #e9f0fd; color: #29485d; justify-self: end; } .message-meta { display: flex; align-items: center; gap: 6px; margin-bottom: 5px; color: #526a70; font-size: 12px; font-weight: 800; letter-spacing: .035em; text-transform: uppercase; } .message.outbound .message-meta { color: #45678b; } .channel-mark { display: inline-grid; width: 17px; height: 17px; place-items: center; border-radius: 50%; background: #dce9f9; color: #3b72bc; font-size: 10px; } .message-body { color: inherit; font-size: 13px; line-height: 1.52; overflow-wrap: anywhere; white-space: pre-wrap; } .message-time { margin-top: 6px; color: #536b71; font-size: 12px; text-align: right; } .event-card { display: flex; align-items: start; gap: 10px; padding: 10px 11px; border: 1px solid #e2e9e7; border-radius: 10px; background: #fbfdfc; color: #4d656c; font-size: 12px; line-height: 1.4; } .event-icon { display: grid; width: 24px; height: 24px; flex: 0 0 auto; place-items: center; border-radius: 7px; background: #eef4f2; color: #527c73; font-size: 13px; } .event-card b { color: #38545d; } .timeline-empty { padding: 22px 0; color: #526b72; font-size: 13px; text-align: center; }
  .record-head { padding: 19px 18px 13px; border-bottom: 1px solid #e7eeec; } .record-name { margin: 0; color: #294650; font-family: "Bona Nova", Georgia, serif; font-size: 28px; font-weight: 400; letter-spacing: -.04em; } .record-subtitle { margin: 5px 0 0; color: #778a8f; font-size: 12px; } .record-status { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 7px; padding: 13px 18px; border-bottom: 1px solid #e7eeec; background: #fdfefd; } .record-status h3 { grid-column: 1 / -1; margin: 0 0 1px; color: #6c8187; font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 10px; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; } .status-card { min-width: 0; padding: 8px 9px; border: 1px solid #e1ebe8; border-radius: 8px; background: #f7faf9; } .status-label { display: block; color: #7d9093; font-size: 9px; font-weight: 800; letter-spacing: .06em; text-transform: uppercase; } .status-value { display: block; margin-top: 3px; color: #48636a; font-size: 11px; line-height: 1.38; overflow-wrap: anywhere; } .status-card.boundary { background: #f7f9fd; } .status-card.unknown { border-style: dashed; background: #fcfcfb; } .record-scroll { max-height: 700px; overflow: auto; padding: 0 18px 24px; } .record-section { padding: 17px 0; border-bottom: 1px solid #e4ece9; } .record-section:last-child { border-bottom: 0; } .record-section h3 { margin: 0 0 10px; color: #6c8187; font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 10px; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; } .identity-grid { display: grid; gap: 8px; } .identity-value { display: flex; gap: 8px; align-items: baseline; color: #395861; font-size: 12px; overflow-wrap: anywhere; } .identity-label { min-width: 56px; color: #86969a; font-size: 10px; font-weight: 800; text-transform: uppercase; } .tag-list { display: flex; flex-wrap: wrap; gap: 6px; } .tag { padding: 4px 7px; border-radius: 999px; background: #eaf1ef; color: #506d74; font-size: 11px; } .compact-list { display: grid; gap: 8px; } .compact-card { padding: 9px 10px; border-left: 2px solid #9fc0b6; background: #f2f7f5; color: #61787e; font-size: 11px; line-height: 1.45; } .compact-card b { display: block; color: #36535c; font-size: 12px; } .compact-card.payment { border-left-color: #d5ae55; background: #fbf8ec; } .compact-card.task-open { border-left-color: #2d78f5; background: #f1f6ff; } .compact-card.note { border-left-color: #c88b6c; background: #fdf6f1; white-space: pre-wrap; overflow-wrap: anywhere; } .field-grid { display: grid; gap: 7px; } .field-row { display: grid; grid-template-columns: minmax(0, .9fr) minmax(0, 1.15fr); gap: 8px; padding: 7px 0; border-bottom: 1px solid #e8efed; font-size: 11px; } .field-row:last-child { border-bottom: 0; } .field-key { color: #778b90; overflow-wrap: anywhere; } .field-value { color: #395861; overflow-wrap: anywhere; text-align: right; } .empty-small { margin: 0; color: #819196; font-size: 11px; line-height: 1.45; } .source-note { margin-top: 7px; color: #98a5a8; font-size: 10px; line-height: 1.4; }
  .status-card { position: relative; appearance: none; width: 100%; min-height: 66px; padding: 10px 30px 10px 11px; border: 1.5px solid #afc4bf; background: #fff; cursor: pointer; text-align: left; box-shadow: 0 1px 0 rgba(38, 72, 78, .04); transition: border-color .16s ease, background .16s ease, box-shadow .16s ease, transform .16s ease; } .status-card:hover { border-color: #688f87; background: #f2f8f6; box-shadow: 0 5px 13px rgba(39, 76, 81, .09); transform: translateY(-1px); } .status-card:active { transform: translateY(0); } .status-card:focus-visible { outline: 2px solid #2d78f5; outline-offset: 2px; } .status-arrow { position: absolute; top: 50%; right: 10px; color: #3c6a62; font-size: 16px; font-weight: 800; transform: translateY(-50%); } .record-section { scroll-margin-top: 10px; } .record-section:focus { outline: none; } .record-section:focus-visible { border-radius: 7px; outline: 2px solid #2d78f5; outline-offset: 3px; } .record-section-heading { display: flex; align-items: baseline; justify-content: space-between; gap: 10px; margin-bottom: 10px; } .record-section-heading h3 { margin: 0; } .record-section-count { color: #819196; font-size: 10px; font-weight: 800; } .record-tabs { display: grid; grid-template-columns: 1fr 1fr; gap: 3px; margin-bottom: 10px; padding: 3px; border: 1px solid #c6d6d2; border-radius: 9px; background: #edf3f1; } .record-tab { border: 1px solid transparent; border-radius: 6px; padding: 7px 8px; background: transparent; color: #5c7379; cursor: pointer; font-size: 11px; font-weight: 800; } .record-tab[aria-pressed="true"] { border-color: #9fb8b2; background: #fff; color: #294f48; box-shadow: 0 2px 6px rgba(38, 72, 78, .08); } .record-tab:focus-visible { outline: 2px solid #2d78f5; outline-offset: 2px; }
  .payment-head-actions { position: relative; } .payment-actions-toggle { display: inline-flex; align-items: center; gap: 5px; border: 1.5px solid #9db4af; border-radius: 7px; padding: 6px 8px; background: #fff; color: #31564f; cursor: pointer; font-size: 10px; font-weight: 800; } .payment-actions-toggle:hover, .payment-actions-toggle[aria-expanded="true"] { border-color: #5f887f; background: #edf6f3; } .payment-actions-toggle:focus-visible, .payment-action:focus-visible { outline: 2px solid #2d78f5; outline-offset: 2px; } .payment-actions-menu { position: absolute; z-index: 5; top: calc(100% + 5px); right: 0; width: 174px; overflow: hidden; border: 1px solid #b8cbc7; border-radius: 9px; background: #fff; box-shadow: 0 12px 30px rgba(34, 66, 73, .18); } .payment-action { display: flex; align-items: center; justify-content: space-between; gap: 8px; width: 100%; padding: 10px 11px; border: 0; border-bottom: 1px solid #e5ecea; background: #fff; color: #31564f; cursor: pointer; font-size: 11px; font-weight: 800; text-align: left; text-decoration: none; } .payment-action:last-child { border-bottom: 0; } .payment-action:hover { background: #f0f7f5; } .payment-action span { color: #80918f; font-size: 9px; font-weight: 700; } .payment-summary { display: flex; align-items: baseline; justify-content: space-between; gap: 8px; margin-bottom: 10px; padding: 10px 11px; border: 1px solid #e1d5af; border-radius: 9px; background: #fbf8ec; color: #6b644d; font-size: 10px; } .payment-summary strong { color: #443f31; font-size: 17px; } .payment-ledger { overflow: hidden; border: 1px solid #d7e2df; border-radius: 9px; background: #fff; } .payment-row { display: grid; grid-template-columns: minmax(72px, 1fr) minmax(72px, .8fr) minmax(72px, .9fr); gap: 7px; align-items: center; padding: 9px 10px; border-bottom: 1px solid #e8efed; color: #506870; font-size: 10px; } .payment-row:last-child { border-bottom: 0; } .payment-row.heading { background: #f4f7f6; color: #77898d; font-size: 9px; font-weight: 800; letter-spacing: .04em; text-transform: uppercase; } .payment-amount { color: #2f4f58; font-weight: 800; } .payment-state { justify-self: start; padding: 3px 5px; border: 1px solid #9bc9b6; border-radius: 5px; background: #eff9f4; color: #2f7459; font-weight: 800; } .payment-state.refunded { border-color: #d7baa9; background: #fbf2ed; color: #9b5c41; } .payment-state.review { border-color: #d8bd73; background: #fff8df; color: #755d20; }
  .show-all { width: 100%; margin-top: 2px; padding: 7px; border: 1px solid #d6e3e0; border-radius: 8px; background: #fff; color: #52717a; cursor: pointer; font-size: 11px; font-weight: 800; } .show-all:hover { background: #f2f7f5; } .composer { display: grid; flex: 0 0 auto; gap: 8px; margin: 0; padding: 13px 16px 15px; border: 0; border-top: 1px solid #d8e6e2; background: #f7fbfa; box-shadow: 0 -8px 20px rgba(28, 61, 70, .04); } .composer label { display: grid; gap: 4px; color: #637980; font-size: 10px; font-weight: 800; letter-spacing: .08em; text-transform: uppercase; } .composer input, .composer textarea, .composer select { width: 100%; border: 1px solid #d5e2df; border-radius: 7px; padding: 8px; background: #fff; color: #294650; font: inherit; font-size: 13px; } .composer textarea { min-height: 74px; max-height: 160px; resize: vertical; } .composer-actions { display:flex; align-items:center; gap:10px; justify-content:space-between; } .composer button { border:0; border-radius:8px; padding:9px 13px; background:#2d78f5; color:#fff; cursor:pointer; font-size:12px; font-weight:800; } .composer button:disabled { cursor:wait; opacity:.65; } .composer-status { color:#687e84; font-size:11px; }
  @media (max-width: 1080px) { .workspace { height: auto; max-height: none; overflow: visible; grid-template-columns: minmax(245px, .72fr) minmax(380px, 1.28fr); } .inbox, .conversation { height: min(66vh, 640px); } .record { max-height: min(70vh, 720px); grid-column: 1 / -1; border-top: 1px solid #e4eceb; } .record-scroll { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0 22px; max-height: none; } }
  @media (max-width: 720px) { main { width: min(100% - 20px, 580px); padding-top: 20px; } .page-head { display: block; } .page-note { margin-top: 10px; text-align: left; } .count { display: none; } .workspace { height: auto; grid-template-columns: 1fr; border-radius: 14px; } .inbox, .conversation { border-right: 0; border-bottom: 1px solid #e4eceb; } .conversation { min-height: 560px; } .record-scroll { display: block; } .timeline { padding: 15px; } .conversation-empty { min-height: 210px; } .payment-row { grid-template-columns: minmax(70px, 1fr) minmax(70px, .85fr) minmax(68px, .9fr); } }
  /* Channel is text in the thread; the blue dot belongs only to unread inbox rows. */
  .message .channel-mark { display: none; }
</style></head><body><main>
<div class="eyebrow">Amari Method · staff</div><div class="page-head"><h1>Communication</h1><p class="page-note">Every mirrored contact, ordered by most recent activity. Client, automated, and operational messages remain visible in the selected record. This mirror does not send messages.</p></div>
<label class="searchbar"><svg viewBox="0 0 24 24" width="18" height="18" aria-hidden="true"><circle cx="11" cy="11" r="6" fill="none" stroke="currentColor" stroke-width="2"/><path d="m16 16 4 4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg><input id="query" type="search" autocomplete="off" placeholder="Search name, email, or phone" aria-label="Search all contacts" /><span class="count" id="count">Loading…</span></label>
<section class="workspace" aria-label="Complete communication workspace"><aside class="pane inbox"><header class="pane-head"><h2 class="pane-title">All contacts</h2><span class="unread" id="unread" aria-live="polite">—</span></header><ul class="thread-list" id="thread-list"></ul></aside><section class="pane conversation" id="conversation" aria-live="polite"><div class="conversation-empty"><div><strong>Select a contact</strong>Read the complete mirrored chronology without leaving the record.</div></div></section><aside class="pane record" id="record" aria-live="polite"><div class="conversation-empty"><div><strong>Contact record</strong>Contact details, appointments, notes, tasks, and payments appear here.</div></div></aside></section>
</main><script>
(() => {
  const list = document.getElementById('thread-list'), conversation = document.getElementById('conversation'), record = document.getElementById('record'), query = document.getElementById('query'), count = document.getElementById('count'), unread = document.getElementById('unread');
  const requestedExternalContact = typeof window === 'undefined' ? null : new URLSearchParams(window.location.search).get('contact');
  let requestedContactOpened = false, selected = null, current = [], timer, inboxRequest = 0, detailRequest = 0, detailController = null;
  const esc = (value) => String(value == null ? '' : value).replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[char]);
  const initials = (name) => String(name || 'Client').split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]).join('').toUpperCase();
  const activityAge = (value) => { if (!value) return '—'; const minutes = Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 60000)); if (minutes < 1) return 'Just now'; if (minutes < 60) return minutes + ' minute' + (minutes === 1 ? '' : 's') + ' ago'; const hours = Math.floor(minutes / 60); if (hours < 24) return hours + ' hour' + (hours === 1 ? '' : 's') + ' ago'; const days = Math.floor(hours / 24); return days < 7 ? days + ' day' + (days === 1 ? '' : 's') + ' ago' : new Date(value).toLocaleDateString(undefined, { month: 'short', day: 'numeric' }); };
  const dateTime = (value) => value ? new Date(value).toLocaleString(undefined, { month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' }) : 'Date unknown';
  const threadTime = (value) => value ? '<time datetime="' + esc(value) + '">' + esc(activityAge(value)) + '<span class="thread-exact-time">' + esc(dateTime(value)) + '</span></time>' : 'No activity';
  const dateOnly = (value) => value ? new Date(value).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' }) : 'Date unknown';
  const money = (amount, currency) => (Number(amount || 0) / 100).toLocaleString(undefined, { style: 'currency', currency: String(currency || 'usd').toUpperCase() });
  const cleanMessage = (value) => { let text = String(value || '').replaceAll('\\n', ' ').replaceAll('\\r', ' '); if (text.startsWith('.ProseMirror')) { const cssEnd = text.indexOf('}'); if (cssEnd >= 0) text = text.slice(cssEnd + 1); } text = text.split(' ').map((word) => word.startsWith('https://') || word.startsWith('http://') ? (word.includes('amarimethod') ? '[Amari link]' : '[link]') : word).join(' '); text = text.split('If you no longer wish to receive these emails')[0].split('Reply STOP to unsubscribe')[0]; return text.split(' ').filter(Boolean).join(' ').trim(); };
  const inboxPreview = (value, maximum = 170) => { const text = cleanMessage(value); if (text.length <= maximum) return text; const boundary = text.lastIndexOf(' ', maximum); return text.slice(0, boundary > 40 ? boundary : maximum).trimEnd() + '…'; };
  function splitUrlToken(token) { let text = token; let prefix = ''; let suffix = ''; while (text.startsWith('[') || text.startsWith('(')) { prefix += text[0]; text = text.slice(1); } while (text && '.,;!?])}'.includes(text.at(-1))) { suffix = text.at(-1) + suffix; text = text.slice(0, -1); } const wrapped = Boolean(prefix) && (suffix.includes(']') || suffix.includes(')') || suffix.includes('}')); return { prefix: wrapped ? '' : prefix, url: text, suffix: wrapped ? suffix.replaceAll(']', '').replaceAll(')', '').replaceAll('}', '') : suffix }; }
  function emailLinkLabel(url, preceding) { const target = url.toLowerCase(), context = preceding.toLowerCase(); if (target.includes('unsubscribe')) return 'Unsubscribe'; if (target.includes('cancel-booking') || (target.includes('booking') && context.includes('cancel'))) return 'Cancel'; if (target.includes('widget/booking') && context.includes('reschedule')) return 'Reschedule'; if (target.includes('google/calendar/add-event')) return 'Add to Google Calendar'; if (target.includes('google/calendar/get-ics')) return 'Add to iCal / Outlook'; if (target.includes('email-tracking') && context.includes('upgrade to 4 sessions')) return 'Upgrade to 4 sessions'; if (target.includes('email-tracking') && context.includes('upgrade to 8 sessions')) return 'Upgrade to 8 sessions'; if (target.includes('email-tracking') && (context.includes('share your feedback') || context.includes('how are you feeling'))) return 'Share feedback'; if (target.includes('email-tracking') && context.includes('book')) return 'Book a session'; if (target.includes('amarimethod.com')) return 'Visit Amari Method'; return 'Open link'; }
  function emailBody(value) { const source = String(value == null ? '' : value).replaceAll('\\n', ' ').replaceAll('\\r', ' '); return source.split(' ').map((token, index, tokens) => { const parts = splitUrlToken(token); if (!parts.url.startsWith('https://') && !parts.url.startsWith('http://')) return esc(token); const preceding = tokens.slice(Math.max(0, index - 16), index).join(' '); return esc(parts.prefix) + '<a class="message-link" href="' + esc(parts.url) + '" target="_blank" rel="noopener noreferrer" referrerpolicy="no-referrer">' + esc(emailLinkLabel(parts.url, preceding)) + '</a>' + esc(parts.suffix); }).join(' '); }
  const labels = { 'system.dnd': 'Do not disturb', 'system.owner': 'Owner', 'system.followers': 'Followers', 'wrQSkx6BhXwDGIn1d0V4': 'Sessions remaining', 'TE0udwVH1Km5RsKaN5H0': 'Sessions completed', '3i93lTkmuAV49s9nh0q8': 'Series type', 'O0xmwyRqeNK2EA1GGGye': 'Portal access', '1EnVtI70jC5MTshZjWvw': 'Living Practice access' };
  const labelKey = (key) => labels[key] || (/^[A-Za-z0-9]{12,}$/.test(String(key || '')) ? 'Additional client field' : String(key || '').replace(/^contact\./, '').replace(/[_.-]+/g, ' ').replace(/\b\w/g, (letter) => letter.toUpperCase()));
  const readableField = (field) => Boolean(field.attribute_value) && (!/^[A-Za-z0-9]{12,}$/.test(String(field.attribute_key || '')) || Boolean(labels[field.attribute_key]));
  function dayMarker(value) { return '<div class="day-line">' + esc(value ? new Date(value).toLocaleDateString(undefined, { weekday: 'short', month: 'short', day: 'numeric' }) : 'Timeline') + '</div>'; }
  function timelineDayKey(value) { return value ? new Date(value).toLocaleDateString(undefined, { year: 'numeric', month: 'numeric', day: 'numeric' }) : 'unknown'; }
  function channelMark(kind) { return kind === 'email' ? '✉' : kind === 'sms' ? '●' : '◷'; }
  function renderThreads() { list.replaceChildren(); const unreadRows = current.filter((row) => Number(row.unread_inbound_count) > 0).length; count.textContent = current.length + ' contact' + (current.length === 1 ? '' : 's'); unread.textContent = unreadRows ? unreadRows + ' needs attention' : 'All caught up'; if (!current.length) { list.innerHTML = '<li class="list-empty">No matching contacts.</li>'; return; } current.forEach((row) => { const item = document.createElement('li'); const button = document.createElement('button'); button.type = 'button'; button.className = 'thread-row'; button.setAttribute('aria-current', String(selected === row.contact_id)); button.innerHTML = '<i class="blue-dot ' + (Number(row.unread_inbound_count) > 0 ? 'unread' : '') + '"></i><span><span class="thread-name">' + esc(row.display_name || 'Unnamed contact') + '</span><span class="thread-preview">' + esc(inboxPreview(row.last_preview || 'No communication mirrored yet.')) + '</span></span><span class="thread-meta">' + esc(row.channel || '—') + '<br>' + threadTime(row.last_event_at) + '</span>'; button.addEventListener('click', () => openClient(row.contact_id)); item.append(button); list.append(item); }); }
  async function loadInbox() { const params = new URLSearchParams({ limit: '1000' }); const text = query.value.trim(); if (text.length >= 2) params.set('query', text); count.textContent = 'Loading…'; try { const response = await fetch('/communications/inbox?' + params, { credentials: 'same-origin' }); if (!response.ok) throw new Error('unavailable'); current = (await response.json()).threads || []; renderThreads(); if (selected && !current.some((row) => row.contact_id === selected)) selected = null; } catch { current = []; list.innerHTML = '<li class="list-empty">Index unavailable. Refresh to try again.</li>'; count.textContent = 'Unavailable'; unread.textContent = '—'; } }
  const timelineFilters = [['all', 'All'], ['messages', 'Messages'], ['appointments', 'Appointments'], ['payments', 'Payments'], ['invoices', 'Invoices'], ['notes', 'Notes'], ['tasks', 'Tasks']];
  function filterTimeline(timeline, filter) { if (filter === 'all') return timeline; const type = filter === 'messages' ? 'message' : filter.slice(0, -1); return timeline.filter((event) => (event.activity_type || 'message') === type); }
  function timelineToolbar(active) { return '<nav class="timeline-toolbar" aria-label="Timeline filters">' + timelineFilters.map(([value, label]) => '<button class="timeline-filter" type="button" data-timeline-filter="' + value + '" aria-pressed="' + String(value === active) + '">' + label + '</button>').join('') + '</nav>'; }
  function timelineMarkup(timeline, filter) { const filtered = filterTimeline(timeline, filter); if (!filtered.length) return '<p class="timeline-empty">No ' + esc(timelineFilters.find(([value]) => value === filter)?.[1].toLowerCase() || 'matching') + ' are mirrored for this contact.</p>'; let lastDay = null; return filtered.map((event) => { const day = timelineDayKey(event.occurred_at); const marker = day === lastDay ? '' : dayMarker(event.occurred_at); lastDay = day; return marker + timelineItem(event); }).join(''); }
  function timelineItem(event) { const kind = event.channel || event.thread_channel || event.event_kind || 'event'; if (event.activity_type && event.activity_type !== 'message') { const detail = event.activity_type === 'payment' ? money(event.amount_cents, event.currency) + ' · ' + (event.body || event.delivery_status || 'payment') : event.activity_type === 'invoice' ? (event.subject || 'Invoice') + ' · ' + money(event.amount_cents, event.currency) + ' · ' + (event.delivery_status || 'recorded') : event.activity_type === 'appointment' ? (event.detail || 'Appointment') + ' · ' + (event.status || 'recorded') : event.activity_type === 'note' ? event.body || 'Note' : (event.subject || 'Task') + ' · ' + (event.status || 'open') + (event.detail ? ' · due ' + dateOnly(event.detail) : ''); return '<article class="event-card"><span class="event-icon">' + (event.activity_type === 'payment' ? '$' : event.activity_type === 'invoice' ? '▣' : event.activity_type === 'appointment' ? '◷' : event.activity_type === 'note' ? '▤' : '✓') + '</span><div><b>' + esc(event.activity_type) + '</b><br>' + esc(cleanMessage(detail)) + '<br><span>' + esc(dateTime(event.occurred_at)) + '</span></div></article>'; } if (kind !== 'sms' && kind !== 'email') return '<article class="event-card"><span class="event-icon">' + channelMark(kind) + '</span><div><b>' + esc(event.event_kind || 'Client event') + '</b><br>' + esc(cleanMessage(event.subject || event.body_clean || 'Recorded event')) + '<br><span>' + esc(dateTime(event.occurred_at)) + '</span></div></article>'; const outgoing = event.direction === 'outbound'; const content = event.body || event.body_clean || event.subject || 'No message content mirrored.'; return '<article class="message ' + (outgoing ? 'outbound' : '') + '"><div class="message-meta"><span class="channel-mark">' + channelMark(kind) + '</span>' + (outgoing ? 'Sent' : 'Client') + ' · ' + esc(kind) + '</div><div class="message-body">' + (kind === 'email' ? emailBody(content) : esc(cleanMessage(content))) + '</div><div class="message-time">' + esc(dateTime(event.occurred_at)) + (event.delivery_status ? ' · ' + esc(event.delivery_status) : '') + '</div></article>'; }
  function compactCards(rows, type) { if (!rows.length) return ''; return rows.map((row) => { if (type === 'appointment') return '<div class="compact-card"><b>' + esc(row.status || 'Recorded appointment') + ' · ' + esc(dateOnly(row.starts_at)) + '</b>' + esc(row.service_name || 'Appointment') + (row.ends_at ? ' · ends ' + esc(new Date(row.ends_at).toLocaleTimeString(undefined, { hour: 'numeric', minute: '2-digit' })) : '') + '</div>'; if (type === 'payment') { const refunded = Number(row.amount_refunded_cents || 0); return '<div class="compact-card payment"><b>' + esc(money(row.amount_cents, row.currency)) + ' · ' + esc(row.provider_status || 'payment') + '</b>' + esc(row.classification || 'Unclassified payment') + ' · ' + esc(dateOnly(row.purchased_at)) + (refunded ? '<br>Refunded ' + esc(money(refunded, row.currency)) : '') + '</div>'; } if (type === 'invoice') { const due = Number(row.amount_due_cents || 0); const paid = Number(row.amount_paid_cents || 0); const remaining = Number(row.amount_remaining_cents || 0); return '<div class="compact-card payment"><b>' + esc(row.invoice_number || 'Stripe invoice') + ' · ' + esc(row.provider_status || 'recorded') + '</b>' + esc(row.description || 'Invoice') + ' · due ' + esc(money(due, row.currency)) + (paid ? ' · paid ' + esc(money(paid, row.currency)) : '') + (remaining ? '<br>Remaining ' + esc(money(remaining, row.currency)) : '') + ' · ' + esc(dateOnly(row.issued_at)) + (row.due_at ? '<br>Due ' + esc(dateOnly(row.due_at)) : '') + '</div>'; } if (type === 'note') return '<div class="compact-card note"><b>' + esc(row.authored_by || 'Note') + ' · ' + esc(dateOnly(row.created_at)) + '</b>' + esc(row.body) + '</div>'; const completed = row.completed_at ? 'completed' : (row.status || 'open'); return '<div class="compact-card ' + (row.completed_at ? '' : 'task-open') + '"><b>' + esc(row.title) + '</b>' + esc(completed) + (row.due_at ? ' · due ' + esc(dateOnly(row.due_at)) : '') + '</div>'; }).join(''); }
  function empty(label, source) { return '<p class="empty-small">No ' + esc(label) + ' mirrored yet.</p>' + (source ? '<p class="source-note">' + esc(source) + '</p>' : ''); }
  function profileMarkup(data) { const c = data.contact || {}; const fields = data.fields || []; const management = Object.fromEntries(fields.filter((field) => String(field.attribute_key || '').startsWith('system.')).map((field) => [field.attribute_key, field.attribute_value])); const visibleFields = fields.filter((field) => field.attribute_value && !String(field.attribute_key || '').startsWith('system.')); const clientState = data.importedCurrentState || {}; const paymentAccess = data.paymentAccess || {}; const timeline = data.activityTimeline || data.communicationTimeline || []; const nextAppointment = (data.appointments || []).filter((appointment) => appointment.starts_at && new Date(appointment.starts_at).getTime() >= Date.now()).sort((a, b) => new Date(a.starts_at) - new Date(b.starts_at))[0]; const statusCards = [['Last mirrored activity', timeline[0]?.occurred_at ? dateTime(timeline[0].occurred_at) : 'Not mirrored', timeline[0]?.occurred_at ? '' : ' unknown'], ['Next appointment', nextAppointment?.starts_at ? dateTime(nextAppointment.starts_at) : 'Not mirrored', nextAppointment?.starts_at ? '' : ' unknown'], ['GHL mirror', 'Contact, appointments, notes, tasks', ' boundary'], ['Stripe mirror', 'Payments and invoices', ' boundary']].map((card) => '<div class="status-card' + card[2] + '"><span class="status-label">' + esc(card[0]) + '</span><span class="status-value">' + esc(card[1]) + '</span></div>').join(''); const dnd = String(management['system.dnd'] || '').toLowerCase() === 'on' ? 'On' : 'Off'; const identities = [['Email', c.email_normalized], ['Mobile', c.phone_e164], ['Owner', management['system.owner'] || 'Not mirrored'], ['Followers', management['system.followers'] || 'Not mirrored'], ['DND', dnd]].map((pair) => '<div class="identity-value"><span class="identity-label">' + esc(pair[0]) + '</span><span>' + esc(pair[1] || '—') + '</span></div>').join(''); const state = [['Series', clientState.series_type], ['Sessions left', clientState.sessions_remaining], ['Sessions done', clientState.sessions_completed], ['Portal access', clientState.portal_access], ['Living Practice', clientState.living_practice_access]].filter((pair) => pair[1] != null && pair[1] !== '').map((pair) => '<div class="identity-value"><span class="identity-label">' + esc(pair[0]) + '</span><span>' + esc(pair[1]) + '</span></div>').join(''); const accessStatus = paymentAccess.status === 'aligned' ? 'access-aligned' : paymentAccess.status === 'review_access_state' ? 'access-review' : 'access-neutral'; const accessSummary = paymentAccess.payment ? [['Stripe payment', paymentAccess.payment.classification || 'Package payment'], ['Current access', paymentAccess.label || 'Not mirrored']].map((pair) => '<div class="identity-value"><span class="identity-label">' + esc(pair[0]) + '</span><span>' + esc(pair[1]) + '</span></div>').join('') : '<p class="empty-small">No linked package payment.</p>'; return '<div class="record-head"><h2 class="record-name">' + esc(c.display_name || 'Unnamed client') + '</h2><p class="record-subtitle">Current client record · read-only mirror</p></div><section class="record-status"><h3>Record status</h3>' + statusCards + '</section><div class="record-scroll"><section class="record-section"><h3>Contact</h3><div class="identity-grid">' + identities + '</div><p class="source-note">DND is shown as on or off. This mirror does not send messages.</p></section><section class="record-section"><h3>Tags</h3><div class="tag-list">' + ((data.tags || []).map((tag) => '<span class="tag">' + esc(tag) + '</span>').join('') || '<span class="empty-small">No tags mirrored.</span>') + '</div></section><section class="record-section"><h3>Payment &amp; access</h3><div class="identity-grid ' + accessStatus + '">' + accessSummary + '</div><p class="source-note">' + esc(paymentAccess.detail || 'Stripe payment evidence and GHL access are mirrored separately. This view never changes either system.') + '</p></section>' + (state ? '<section class="record-section"><h3>Current plan</h3><div class="identity-grid">' + state + '</div></section>' : '') + '<section class="record-section"><h3>Appointments</h3><div class="compact-list">' + (compactCards(data.appointments || [], 'appointment') || empty('appointments')) + '</div></section><section class="record-section"><h3>Payments</h3><div class="compact-list">' + (compactCards(data.purchases || [], 'payment') || empty('payments', 'Settled payments are mirrored from Stripe; unlinked payments are intentionally not shown on a client record.')) + '</div></section><section class="record-section"><h3>Invoices</h3><div class="compact-list">' + (compactCards(data.invoices || [], 'invoice') || empty('invoices', 'Invoices are mirrored from Stripe when their source customer relationship is unambiguous.')) + '</div></section><section class="record-section"><h3>Notes</h3><div class="compact-list">' + (compactCards(data.notes || [], 'note') || empty('notes', 'New GHL notes will appear as their webhook events arrive.')) + '</div></section><section class="record-section"><h3>Tasks</h3><div class="compact-list">' + (compactCards(data.tasks || [], 'task') || empty('tasks', 'New GHL tasks will appear as their webhook events arrive.')) + '</div></section><section class="record-section"><h3>Contact fields</h3><div class="field-grid" id="field-grid">' + (visibleFields.slice(0, 9).map((field) => '<div class="field-row"><span class="field-key">' + esc(labelKey(field.attribute_key)) + '</span><span class="field-value">' + esc(field.attribute_value) + '</span></div>').join('') || '<p class="empty-small">No additional fields mirrored.</p>') + '</div>' + (visibleFields.length > 9 ? '<button class="show-all" type="button" id="show-fields">Show all ' + visibleFields.length + ' fields</button>' : '') + '</section></div>'; }
  function appointmentHistoryMarkup(rows) {
    const dated = rows.filter((row) => row.starts_at);
    const upcoming = dated.filter((row) => new Date(row.starts_at).getTime() >= Date.now()).sort((a, b) => new Date(a.starts_at) - new Date(b.starts_at));
    const past = dated.filter((row) => new Date(row.starts_at).getTime() < Date.now()).sort((a, b) => new Date(b.starts_at) - new Date(a.starts_at));
    const initial = upcoming.length || !past.length ? 'upcoming' : 'past';
    return '<div class="record-tabs" aria-label="Appointment history">' +
      '<button class="record-tab" type="button" data-appointment-tab="upcoming" aria-controls="appointment-upcoming" aria-pressed="' + String(initial === 'upcoming') + '">Upcoming · ' + upcoming.length + '</button>' +
      '<button class="record-tab" type="button" data-appointment-tab="past" aria-controls="appointment-past" aria-pressed="' + String(initial === 'past') + '">Past · ' + past.length + '</button></div>' +
      '<div id="appointment-upcoming" data-appointment-panel="upcoming"' + (initial === 'upcoming' ? '' : ' hidden') + '><div class="compact-list">' + (compactCards(upcoming, 'appointment') || empty('upcoming appointments')) + '</div></div>' +
      '<div id="appointment-past" data-appointment-panel="past"' + (initial === 'past' ? '' : ' hidden') + '><div class="compact-list">' + (compactCards(past, 'appointment') || empty('past appointments')) + '</div></div>';
  }
  function paymentFacts(rows) {
    const settled = rows.filter((row) => ['paid', 'succeeded'].includes(String(row.provider_status || '').toLowerCase()));
    const spentCents = settled.reduce((total, row) => total + Math.max(0, Number(row.amount_cents || 0) - Number(row.amount_refunded_cents || 0)), 0);
    const currency = settled[0]?.currency || rows[0]?.currency || 'usd';
    const dated = rows.filter((row) => row.purchased_at).sort((a, b) => new Date(a.purchased_at) - new Date(b.purchased_at));
    return { spentCents, currency, since: dated[0]?.purchased_at || null };
  }
  function paymentWorkspaceMarkup(rows, contact) {
    const facts = paymentFacts(rows);
    const posContact = contact.ghl_contact_id;
    const posBase = posContact ? 'https://www.amarimethod.com/staff/pos?contact=' + encodeURIComponent(posContact) : 'https://www.amarimethod.com/staff/pos';
    const handoffData = posContact
      ? ' data-contact-id="' + esc(posContact) + '" data-contact-name="' + esc(contact.display_name || 'Practice member') + '" data-contact-email="' + esc(contact.email_normalized || '') + '" data-contact-phone="' + esc(contact.phone_e164 || '') + '"'
      : '';
    const actionMenu = '<div class="payment-head-actions"><button class="payment-actions-toggle" type="button" data-payment-actions aria-expanded="false" aria-controls="payment-actions-menu">Actions <span aria-hidden="true">⌄</span></button>' +
      '<div class="payment-actions-menu" id="payment-actions-menu" hidden>' +
      '<a class="payment-action" href="' + esc(posBase + (posContact ? '&action=charge' : '')) + '" target="_top" data-staff-handoff' + handoffData + '>Charge now <span>Staff POS →</span></a>' +
      '<a class="payment-action" href="' + esc(posBase) + '" target="_top" data-staff-handoff' + handoffData + '>Open full POS <span>New sale →</span></a></div></div>';
    const rowsMarkup = rows.map((row) => { const status = String(row.provider_status || 'recorded'); const refunded = Number(row.amount_refunded_cents || 0); const review = row.identity_status === 'match_review'; return '<div class="payment-row" role="row"><span role="cell">' + esc(dateOnly(row.purchased_at)) + '</span><span class="payment-amount" role="cell">' + esc(money(row.amount_cents, row.currency)) + (refunded ? '<br><small>−' + esc(money(refunded, row.currency)) + ' refunded</small>' : '') + '</span><span class="payment-state ' + (refunded ? 'refunded' : review ? 'review' : '') + '" role="cell">' + esc(refunded ? 'Refunded' : review ? 'Match review' : status) + '</span></div>'; }).join('');
    return '<div class="record-section-heading"><h3>Payments</h3>' + actionMenu + '</div>' +
      '<div class="payment-summary"><span>Stripe evidence' + (facts.since ? '<br>Since ' + esc(dateOnly(facts.since)) : '') + '</span><strong>' + esc(money(facts.spentCents, facts.currency)) + '</strong></div>' +
      (rows.length ? '<div class="payment-ledger" role="table" aria-label="Payment transactions"><div class="payment-row heading" role="row"><span role="columnheader">Date</span><span role="columnheader">Amount</span><span role="columnheader">Status</span></div>' + rowsMarkup + '</div><p class="source-note">Stripe is the money source of truth. Match review means unique contact evidence is visible here but is not yet used for access or session records.</p>' : empty('payments', 'No Stripe charge is linked or uniquely matched to this contact yet.'));
  }
  function statusLauncher(label, value, target, controls) {
    return '<button class="status-card" type="button" data-record-target="' + esc(target) + '" aria-controls="' + esc(controls) + '"><span class="status-label">' + esc(label) + '</span><span class="status-value">' + esc(value) + '</span><span class="status-arrow" aria-hidden="true">→</span></button>';
  }
  function profileMarkup(data) {
    const c = data.contact || {};
    const fields = data.fields || [];
    const management = Object.fromEntries(fields.filter((field) => String(field.attribute_key || '').startsWith('system.')).map((field) => [field.attribute_key, field.attribute_value]));
    const visibleFields = fields.filter((field) => field.attribute_value && !String(field.attribute_key || '').startsWith('system.'));
    const clientState = data.importedCurrentState || {};
    const paymentAccess = data.paymentAccess || {};
    const timeline = data.activityTimeline || data.communicationTimeline || [];
    const appointments = data.appointments || [];
    const purchases = data.purchases || [];
    const paymentRows = purchases.concat(data.purchaseCandidates || []).sort((a, b) => String(b.purchased_at || '').localeCompare(String(a.purchased_at || '')));
    const invoices = data.invoices || [];
    const notes = data.notes || [];
    const paymentSummary = paymentFacts(paymentRows);
    const nextAppointment = appointments.filter((appointment) => appointment.starts_at && new Date(appointment.starts_at).getTime() >= Date.now()).sort((a, b) => new Date(a.starts_at) - new Date(b.starts_at))[0];
    const statusCards = [
      statusLauncher('Activity', timeline[0]?.occurred_at ? dateTime(timeline[0].occurred_at) : 'No activity mirrored', 'activity', 'conversation'),
      statusLauncher('Appointments', nextAppointment?.starts_at ? 'Next · ' + dateTime(nextAppointment.starts_at) : appointments.length + ' mirrored', 'appointments', 'record-appointments'),
      statusLauncher('Payments', money(paymentSummary.spentCents, paymentSummary.currency) + ' Stripe evidence · ' + paymentRows.length + ' transaction' + (paymentRows.length === 1 ? '' : 's'), 'payments', 'record-payments'),
      statusLauncher('Notes', notes.length + ' note' + (notes.length === 1 ? '' : 's'), 'notes', 'record-notes'),
    ].join('');
    const dnd = String(management['system.dnd'] || '').toLowerCase() === 'on' ? 'On' : 'Off';
    const identities = [['Email', c.email_normalized], ['Mobile', c.phone_e164], ['Owner', management['system.owner'] || 'Not mirrored'], ['Followers', management['system.followers'] || 'Not mirrored'], ['DND', dnd]].map((pair) => '<div class="identity-value"><span class="identity-label">' + esc(pair[0]) + '</span><span>' + esc(pair[1] || '—') + '</span></div>').join('');
    const state = [['Series', clientState.series_type], ['Sessions left', clientState.sessions_remaining], ['Sessions done', clientState.sessions_completed], ['Portal access', clientState.portal_access], ['Living Practice', clientState.living_practice_access]].filter((pair) => pair[1] != null && pair[1] !== '').map((pair) => '<div class="identity-value"><span class="identity-label">' + esc(pair[0]) + '</span><span>' + esc(pair[1]) + '</span></div>').join('');
    const accessStatus = paymentAccess.status === 'aligned' ? 'access-aligned' : paymentAccess.status === 'review_access_state' ? 'access-review' : 'access-neutral';
    const accessSummary = paymentAccess.payment ? [['Stripe payment', paymentAccess.payment.classification || 'Package payment'], ['Current access', paymentAccess.label || 'Not mirrored']].map((pair) => '<div class="identity-value"><span class="identity-label">' + esc(pair[0]) + '</span><span>' + esc(pair[1]) + '</span></div>').join('') : '<p class="empty-small">No linked package payment.</p>';
    return '<div class="record-head"><h2 class="record-name">' + esc(c.display_name || 'Unnamed client') + '</h2><p class="record-subtitle">Current client record · read-only mirror</p></div>' +
      '<section class="record-status"><h3>Open record</h3>' + statusCards + '</section>' +
      '<div class="record-scroll"><section class="record-section"><h3>Contact</h3><div class="identity-grid">' + identities + '</div><p class="source-note">DND is shown as on or off. This mirror does not send messages.</p></section>' +
      '<section class="record-section"><h3>Tags</h3><div class="tag-list">' + ((data.tags || []).map((tag) => '<span class="tag">' + esc(tag) + '</span>').join('') || '<span class="empty-small">No tags mirrored.</span>') + '</div></section>' +
      '<section class="record-section"><h3>Payment &amp; access</h3><div class="identity-grid ' + accessStatus + '">' + accessSummary + '</div><p class="source-note">' + esc(paymentAccess.detail || 'Stripe payment evidence and GHL access are mirrored separately. This view never changes either system.') + '</p></section>' +
      (state ? '<section class="record-section"><h3>Current plan</h3><div class="identity-grid">' + state + '</div></section>' : '') +
      '<section class="record-section" id="record-appointments" tabindex="-1"><div class="record-section-heading"><h3>Appointments</h3><span class="record-section-count">' + appointments.length + ' total</span></div>' + appointmentHistoryMarkup(appointments) + '</section>' +
      '<section class="record-section" id="record-payments" tabindex="-1">' + paymentWorkspaceMarkup(paymentRows, c) + '</section>' +
      '<section class="record-section"><h3>Invoices</h3><div class="compact-list">' + (compactCards(invoices, 'invoice') || empty('invoices', 'Invoices are mirrored from Stripe when their source customer relationship is unambiguous.')) + '</div></section>' +
      '<section class="record-section" id="record-notes" tabindex="-1"><div class="record-section-heading"><h3>Notes</h3><span class="record-section-count">' + notes.length + ' total</span></div><div class="compact-list">' + (compactCards(notes, 'note') || empty('notes', 'New GHL notes will appear as their webhook events arrive.')) + '</div></section>' +
      '<section class="record-section"><h3>Tasks</h3><div class="compact-list">' + (compactCards(data.tasks || [], 'task') || empty('tasks', 'New GHL tasks will appear as their webhook events arrive.')) + '</div></section>' +
      '<section class="record-section"><h3>Contact fields</h3><div class="field-grid" id="field-grid">' + (visibleFields.slice(0, 9).map((field) => '<div class="field-row"><span class="field-key">' + esc(labelKey(field.attribute_key)) + '</span><span class="field-value">' + esc(field.attribute_value) + '</span></div>').join('') || '<p class="empty-small">No additional fields mirrored.</p>') + '</div>' + (visibleFields.length > 9 ? '<button class="show-all" type="button" id="show-fields">Show all ' + visibleFields.length + ' fields</button>' : '') + '</section></div>';
  }
  function bindRecordNavigation() {
    const reducedMotion = window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;
    record.querySelectorAll('[data-record-target]').forEach((button) => button.addEventListener('click', () => {
      const targetName = button.dataset.recordTarget;
      if (targetName === 'activity') {
        conversation.querySelector('[data-timeline-filter="all"]')?.click();
        conversation.querySelector('.timeline-scroll')?.scrollTo({ top: 0, behavior: reducedMotion ? 'auto' : 'smooth' });
        return;
      }
      const target = record.querySelector('#record-' + targetName);
      target?.scrollIntoView({ behavior: reducedMotion ? 'auto' : 'smooth', block: 'start' });
      target?.focus({ preventScroll: true });
    }));
    record.querySelectorAll('[data-appointment-tab]').forEach((button) => button.addEventListener('click', () => {
      const selectedTab = button.dataset.appointmentTab;
      record.querySelectorAll('[data-appointment-tab]').forEach((tab) => tab.setAttribute('aria-pressed', String(tab === button)));
      record.querySelectorAll('[data-appointment-panel]').forEach((panel) => { panel.hidden = panel.dataset.appointmentPanel !== selectedTab; });
    }));
    const paymentActions = record.querySelector('[data-payment-actions]');
    const paymentMenu = record.querySelector('#payment-actions-menu');
    paymentActions?.addEventListener('click', () => {
      const expanded = paymentActions.getAttribute('aria-expanded') === 'true';
      paymentActions.setAttribute('aria-expanded', String(!expanded));
      if (paymentMenu) paymentMenu.hidden = expanded;
    });
    record.querySelectorAll('[data-staff-handoff]').forEach((link) => link.addEventListener('click', (event) => {
      if (window.parent === window) return;
      const parentOrigin = new URLSearchParams(window.location.search).get('parent_origin');
      if (parentOrigin !== 'https://www.amarimethod.com') return;
      const destination = new URL(link.href);
      if (destination.origin !== parentOrigin || destination.pathname !== '/staff/pos') return;
      event.preventDefault();
      const client = link.dataset.contactId ? {
        id: link.dataset.contactId,
        name: link.dataset.contactName || 'Practice member',
        email: link.dataset.contactEmail || null,
        phone: link.dataset.contactPhone || null,
      } : null;
      window.parent.postMessage({ type: 'amari:staff-navigate', path: destination.pathname + destination.search, client }, parentOrigin);
    }));
  }
  async function openClient(contactId) { selected = contactId; renderThreads(); conversation.innerHTML = '<div class="conversation-empty"><div><strong>Loading conversation</strong>Getting the client record.</div></div>'; record.innerHTML = '<div class="conversation-empty"><div><strong>Loading client record</strong></div></div>'; try { const response = await fetch('/client-desk/contacts/' + encodeURIComponent(contactId) + '?limit=50', { credentials: 'same-origin' }); if (!response.ok) throw new Error('unavailable'); const data = await response.json(), c = data.contact || {}; const timeline = data.activityTimeline || data.communicationTimeline || []; let activeTimelineFilter = 'all'; conversation.innerHTML = '<header class="client-head"><div class="client-head-row"><span class="avatar">' + esc(initials(c.display_name)) + '</span><div><h2 class="client-name">' + esc(c.display_name || 'Unnamed client') + '</h2><p class="client-address">' + esc(c.email_normalized || c.phone_e164 || 'Client record') + '</p></div></div><p class="send-state"><b>Email ready.</b> Sending requires this staff action and is blocked for email opt-out or DND.</p></header>' + timelineToolbar(activeTimelineFilter) + '<div class="timeline-scroll"><div class="timeline">' + timelineMarkup(timeline, activeTimelineFilter) + '</div></div>' + emailComposer(c, contactId); record.innerHTML = profileMarkup(data); bindRecordNavigation(); conversation.querySelectorAll('[data-timeline-filter]').forEach((button) => button.addEventListener('click', () => { activeTimelineFilter = button.dataset.timelineFilter; conversation.querySelector('.timeline').innerHTML = timelineMarkup(timeline, activeTimelineFilter); conversation.querySelectorAll('[data-timeline-filter]').forEach((item) => item.setAttribute('aria-pressed', String(item === button))); })); bindEmailComposer(contactId); const expand = document.getElementById('show-fields'); if (expand) expand.addEventListener('click', () => { document.getElementById('field-grid').innerHTML = visibleFieldMarkup(data.fields || []); expand.remove(); }); } catch { conversation.innerHTML = '<div class="conversation-empty"><div><strong>Conversation unavailable</strong>Refresh to try again.</div></div>'; record.innerHTML = '<div class="conversation-empty"><div><strong>Client record unavailable</strong></div></div>'; } }
  function visibleFieldMarkup(fields) { return fields.filter((field) => field.attribute_value && !String(field.attribute_key || '').startsWith('system.')).map((field) => '<div class="field-row"><span class="field-key">' + esc(labelKey(field.attribute_key)) + '</span><span class="field-value">' + esc(field.attribute_value) + '</span></div>').join('') || '<p class="empty-small">No additional fields mirrored.</p>'; }
  // Each contact appears once in this person-level inbox. A dot is an owned
  // attention marker: it clears only after this signed-in staff member opens
  // the selected record, and returns on a newer inbound source event.
  function renderThreads() {
    list.replaceChildren();
    const attentionRows = current.filter((row) => Number(row.unread_inbound_count) > 0).length;
    count.textContent = current.length + ' contact' + (current.length === 1 ? '' : 's');
    unread.textContent = attentionRows ? attentionRows + ' needs attention' : 'All caught up';
    if (!current.length) {
      list.innerHTML = '<li class="list-empty">No matching contacts.</li>';
      return;
    }
    current.forEach((row) => {
      const item = document.createElement('li');
      const button = document.createElement('button');
      button.type = 'button';
      button.className = 'thread-row';
      button.setAttribute('aria-current', String(selected === row.contact_id));
      button.innerHTML = '<i class="blue-dot ' + (Number(row.unread_inbound_count) > 0 ? 'unread' : '') + '"></i><span><span class="thread-name">' + esc(row.display_name || 'Unnamed contact') + '</span><span class="thread-preview">' + esc(inboxPreview(row.last_preview || 'No communication mirrored yet.')) + '</span></span><span class="thread-meta">' + esc(row.channel || '—') + '<br>' + threadTime(row.last_event_at) + '</span>';
      button.addEventListener('click', () => openClient(row.contact_id));
      item.append(button);
      list.append(item);
    });
  }

  async function loadInbox() {
    const params = new URLSearchParams({ limit: '1000' });
    const text = query.value.trim();
    const requestId = ++inboxRequest;
    if (text.length === 1) {
      current = [];
      list.innerHTML = '<li class="list-empty">Type at least 2 characters to search.</li>';
      count.textContent = 'Keep typing…';
      unread.textContent = '—';
      return;
    }
    if (text.length >= 2) params.set('query', text);
    count.textContent = 'Loading…';
    try {
      const response = await fetch('/communications/inbox?' + params, { credentials: 'same-origin' });
      if (!response.ok) throw new Error('unavailable');
      const data = await response.json();
      if (requestId !== inboxRequest) return;
      current = data.threads || [];
      if (selected && !current.some((row) => row.contact_id === selected)) selected = null;
      renderThreads();
      if (!requestedContactOpened && requestedExternalContact) {
        const requested = current.find((row) => row.external_contact_id === requestedExternalContact || row.contact_id === requestedExternalContact);
        if (requested) {
          requestedContactOpened = true;
          await openClient(requested.contact_id);
        }
      }
    } catch (error) {
      if (requestId !== inboxRequest) return;
      current = [];
      list.innerHTML = '<li class="list-empty" role="alert">Index unavailable. Refresh to try again.</li>';
      count.textContent = 'Unavailable';
      unread.textContent = '—';
    }
  }

  function timelineItem(event) {
    const kind = event.channel || event.thread_channel || event.event_kind || 'event';
    if (event.activity_type && event.activity_type !== 'message') {
      const detail = event.activity_type === 'payment' ? money(event.amount_cents, event.currency) + ' · ' + (event.body || event.delivery_status || 'payment') : event.activity_type === 'invoice' ? (event.subject || 'Invoice') + ' · ' + money(event.amount_cents, event.currency) + ' · ' + (event.delivery_status || 'recorded') : event.activity_type === 'appointment' ? (event.detail || 'Appointment') + ' · ' + (event.status || 'recorded') : event.activity_type === 'note' ? event.body || 'Note' : (event.subject || 'Task') + ' · ' + (event.status || 'open') + (event.detail ? ' · due ' + dateOnly(event.detail) : '');
      return '<article class="event-card"><span class="event-icon">' + (event.activity_type === 'payment' ? '$' : event.activity_type === 'invoice' ? '▣' : event.activity_type === 'appointment' ? '◷' : event.activity_type === 'note' ? '▤' : '✓') + '</span><div><b>' + esc(event.activity_type) + '</b><br>' + esc(cleanMessage(detail)) + '<br><span>' + esc(dateTime(event.occurred_at)) + '</span></div></article>';
    }
    if (kind !== 'sms' && kind !== 'email') return '<article class="event-card"><span class="event-icon">' + channelMark(kind) + '</span><div><b>' + esc(event.event_kind || 'Client event') + '</b><br>' + esc(cleanMessage(event.subject || event.body_clean || 'Recorded event')) + '<br><span>' + esc(dateTime(event.occurred_at)) + '</span></div></article>';
    const outgoing = event.direction === 'outbound';
    const notSent = String(event.delivery_status || '').startsWith('not_sent_');
    const speaker = notSent ? 'Not sent' : outgoing ? 'Sent' : event.direction === 'inbound' ? 'Client' : 'Unclassified message';
    const deliveryLabel = event.delivery_status === 'not_sent_policy_blocked' ? 'Policy blocked' : event.delivery_status === 'not_sent_delivery_unavailable' ? 'Delivery unavailable' : event.delivery_status;
    const messageReference = event.message_ref ? ' · ' + esc(event.message_ref) : '';
    const content = event.body || event.body_clean || event.subject || 'No message content mirrored.';
    return '<article class="message ' + (outgoing ? 'outbound' : '') + '"><div class="message-meta"><span class="channel-mark">' + channelMark(kind) + '</span>' + speaker + ' · ' + esc(kind) + '</div><div class="message-body">' + (kind === 'email' ? emailBody(content) : esc(cleanMessage(content))) + '</div><div class="message-time">' + esc(dateTime(event.occurred_at)) + (deliveryLabel ? ' · ' + esc(deliveryLabel) : '') + messageReference + '</div></article>';
  }

  async function openClient(contactId) {
    selected = contactId;
    renderThreads();
    const requestId = ++detailRequest;
    detailController?.abort();
    detailController = new AbortController();
    conversation.innerHTML = '<div class="conversation-empty" role="status"><div><strong>Loading conversation</strong>Getting the client record.</div></div>';
    record.innerHTML = '<div class="conversation-empty" role="status"><div><strong>Loading client record</strong></div></div>';
    try {
      const response = await fetch('/client-desk/contacts/' + encodeURIComponent(contactId) + '?limit=1000', { credentials: 'same-origin', signal: detailController.signal });
      if (!response.ok) throw new Error('unavailable');
      const data = await response.json();
      if (requestId !== detailRequest || selected !== contactId) return;
      const c = data.contact || {};
      const timeline = data.activityTimeline || data.communicationTimeline || [];
      let activeTimelineFilter = 'all';
      conversation.innerHTML = '<header class="client-head"><div class="client-head-row"><span class="avatar">' + esc(initials(c.display_name)) + '</span><div><h2 class="client-name">' + esc(c.display_name || 'Unnamed client') + '</h2><p class="client-address">' + esc(c.email_normalized || c.phone_e164 || 'Client record') + '</p></div></div><p class="send-state">Messages are mirrored here. Sending stays in the approved staff channel.</p></header>' + timelineToolbar(activeTimelineFilter) + '<div class="timeline-scroll"><div class="timeline">' + timelineMarkup(timeline, activeTimelineFilter) + '</div></div>';
      // The API supplies the authoritative next booked/confirmed appointment.
      // Do not let an earlier cancelled future hold replace it in the record UI.
      const profileData = {
        ...data,
        appointments: (data.appointments || []).filter((appointment) => appointment.status === 'booked' || appointment.status === 'confirmed' || !appointment.starts_at || new Date(appointment.starts_at).getTime() < Date.now()),
      };
      record.innerHTML = profileMarkup(profileData);
      bindRecordNavigation();
      conversation.querySelectorAll('[data-timeline-filter]').forEach((button) => button.addEventListener('click', () => {
        activeTimelineFilter = button.dataset.timelineFilter;
        conversation.querySelector('.timeline').innerHTML = timelineMarkup(timeline, activeTimelineFilter);
        conversation.querySelectorAll('[data-timeline-filter]').forEach((item) => item.setAttribute('aria-pressed', String(item === button)));
      }));
      const expand = document.getElementById('show-fields');
      if (expand) expand.addEventListener('click', () => { document.getElementById('field-grid').innerHTML = visibleFieldMarkup(data.fields || []); expand.remove(); });
      const seen = await fetch('/client-desk/contacts/' + encodeURIComponent(contactId) + '/seen', { method: 'POST', credentials: 'same-origin' });
      if (seen.ok && requestId === detailRequest && selected === contactId) {
        current = current.map((row) => row.contact_id === contactId ? { ...row, unread_inbound_count: 0 } : row);
        renderThreads();
      }
    } catch (error) {
      if (requestId !== detailRequest || error?.name === 'AbortError') return;
      conversation.innerHTML = '<div class="conversation-empty" role="alert"><div><strong>Conversation unavailable</strong>Refresh to try again.</div></div>';
      record.innerHTML = '<div class="conversation-empty" role="alert"><div><strong>Client record unavailable</strong></div></div>';
    }
  }

  query.addEventListener('input', () => { clearTimeout(timer); timer = setTimeout(loadInbox, 180); }); loadInbox();
})();
</script></body></html>`;

export function clientDeskHtml() {
  return CLIENT_DESK_HTML;
}
