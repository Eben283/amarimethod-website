{
  "name": "amari-crm-mirror-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only GHL and Stripe mirror for the Amari internal CRM. It never sends messages or writes to providers.",
  "scripts": {
    "test": "vitest run --config vitest.config.js"
  }
}
