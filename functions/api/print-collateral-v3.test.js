import { readFileSync } from 'node:fs';import { describe,expect,it } from 'vitest';const names=['index.html','business-card.html','postcard.html','flyer.html','amarimethod-com-qr.svg'];const source='print-proofs/2026-08-v3';const dist='dist/print-proofs/2026-08-v3';describe('collateral v3 proof contract',()=>{it('keeps source and Cloudflare output identical',()=>{for(const name of names)expect(readFileSync(`${source}/${name}`,'utf8')).toBe(readFileSync(`${dist}/${name}`,'utf8'))});it('pins approved GitHub dependencies and removes local paths',()=>{for(const name of ['business-card.html','postcard.html','flyer.html']){const body=readFileSync(`${source}/${name}`,'utf8');expect(body).toContain('0e57e5279d6ee22670398bb14ca267faa6592070');expect(body).not.toContain('file:///');expect(body).not.toContain('hands-on')}});it('moves the literal card description to the front and restores the back spacing',()=>{const card=readFileSync(`${source}/business-card.html`,'utf8');const front=card.slice(card.indexOf('<section class="card front"'),card.indexOf('<section class="card back"'));const back=card.slice(card.indexOf('<section class="card back"'));expect(front).toContain('<p class="descriptor">Private guided movement for pain and restriction.</p>');expect(back).not.toContain('Private guided movement for pain and restriction.');expect(back).toContain('Subtle input.<br>Unmistakable results.');expect(back).not.toContain('Unmistakable result.');expect(card.match(/Private guided movement for pain and restriction\./g)).toHaveLength(1);expect(card).toContain('.card{width:3.75in;height:2.25in');expect(card).toContain('.front{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:.18in');expect(card).toContain('.descriptor{margin:0;color:rgba(244,243,238,.82);font-size:8.5pt;line-height:1.12');expect(card).toContain('.promise{max-width:14.4ch;margin:.12in 0 auto;font-size:11.5pt');expect(card).toContain('.location{position:absolute;top:.375in;right:.375in;margin:0;color:var(--stone);font-size:7pt');expect(card).toContain('.name{font-size:10pt');expect(card).toContain('.contacts{display:grid;gap:3px;margin-top:6px;font-size:8pt');expect(card).toContain('.site{margin-top:7px;font-size:8.5pt')});it('uses selected image only where intended',()=>{for(const name of ['postcard.html','flyer.html'])expect(readFileSync(`${source}/${name}`,'utf8')).toContain('amari-method-elbow-reset-athletic-ceo.png');expect(readFileSync(`${source}/business-card.html`,'utf8')).not.toContain('amari-method-elbow-reset-athletic-ceo.png')})});