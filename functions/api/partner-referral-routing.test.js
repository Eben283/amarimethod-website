import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{resolvePartnerReferral,recordPartnerReferralAttribution}from"../lib/partner-referrals.js";describe("Vital Ice partner flyer",()=>{it("keeps source/dist exact and visibly identifies the business",()=>{for(const n of["vital-ice-flyer.html","amari-method-vital-ice-qr-vi-001.svg"])expect(readFileSync("print-proofs/2026-08-v3/partners/"+n,"utf8")).toBe(readFileSync("dist/print-proofs/2026-08-v3/partners/"+n,"utf8"));const f=readFileSync("print-proofs/2026-08-v3/partners/vital-ice-flyer.html","utf8");expect(f).toContain("VITAL ICE × AMARI METHOD");expect(f).toContain("PARTNER VI-001");expect(f).not.toMatch(/postcard/i)});it("hands the stable route to the $29 Assessment, not the quiz",()=>{const r=readFileSync("r/vital-ice.html","utf8");expect(r).toContain("/assessment-booking?assessment=1&partner=vital-ice");expect(r).not.toContain("/quiz")});it("resolves only the opaque business id",()=>{expect(resolvePartnerReferral("vital-ice")).toMatchObject({businessName:"Vital Ice",publicCode:"VI-001"});expect(resolvePartnerReferral("sean")).toBeNull()});it("preserves first attribution",async()=>{const rows=new Map(),db={prepare(sql){return{bind(...a){return{async run(){if(sql.includes("INSERT OR IGNORE")&&!rows.has(a[0]))rows.set(a[0],{reward_id:a[1],detail:a[5]});return{}},async first(){return rows.get(a[0])||null}}}}}};const p=resolvePartnerReferral("vital-ice"),a=await recordPartnerReferralAttribution(db,{partner:p,contactId:"c1",referredAt:1}),b=await recordPartnerReferralAttribution(db,{partner:p,contactId:"c1",referredAt:2});expect(a.state).toBe("attributed");expect(b.state).toBe("existing")})});