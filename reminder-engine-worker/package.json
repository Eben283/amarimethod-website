{
  "name": "reminder-engine-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Code-side reminder/confirmation engine — GHL session-reminder workflow replacement (twin migration Unit A). Shadow-safe: computes and logs would-sends until switched to active per flow."
}
