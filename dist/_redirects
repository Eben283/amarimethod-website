# SPA fallback for quiz - serves index.html for all /quiz/* routes
/quiz/* /quiz/index.html 200

# SPA fallback for client portal
/portal/* /portal/index.html 200

# SPA fallback for staff dashboard
# Target is /staff/ (not /staff/index.html) to avoid Cloudflare's
# automatic trailing-slash canonicalization turning the rewrite into
# a 308 redirect to /staff/.
/staff/* /staff/ 200

# SPA fallback for chief of staff
/cos/* /cos/index.html 200

# Optional: Redirect old subdomain to new path (activate after DNS update)
# https://quiz.amarimethod.com/* https://www.amarimethod.com/quiz/:splat 301
