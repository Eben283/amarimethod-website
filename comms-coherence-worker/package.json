{
  "name": "comms-coherence-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker cron that flags cross-channel comms incoherence (redundant/contradictory/confusing messaging, bad timing) per contact via Claude, complementing qa-audit.js's deterministic automation checks",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node ../scripts/worker-release.mjs --worker comms-coherence",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
